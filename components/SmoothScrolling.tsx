"use client";
import React from "react";

interface SmoothScrolling {
  readonly children: React.ReactNode
}

export function SmoothScrolling({ children }: SmoothScrolling) {
  return
  <>
    {children}
  </>
}